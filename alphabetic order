function alpha(str)
{ 
  console.log("before string "  + "  "  +str);
   let str1 = str.split("");
  let straplhabetic = str1.sort();
  console.log("after reverse" + "  " + straplhabetic);
  
  
}
alpha('webmaster');
